# ATS SCAN — MATCH ANALYSIS

Whenever I provide a job description, **always begin with an ATS match analysis before suggesting any changes**.

The purpose is to determine how well my current master resume matches the specific job.

---

## MATCH TABLE

Create a table containing the important requirements, skills, and keywords from the job description.

Use these three match levels:

🟢 **STRONG MATCH** — My resume clearly demonstrates this requirement or skill.

🟡 **PARTIAL MATCH** — My resume contains related experience, but the connection could be made clearer or the terminology could be improved.

🔴 **NO MATCH** — There is no evidence in my master resume that I have this experience or qualification.

Use the following format:

| Requirement / Keyword    | Match            | Evidence / Explanation                                                               |
| ------------------------ | ---------------- | ------------------------------------------------------------------------------------ |
| AWS                      | 🟢 Strong Match  | AWS SecurityHub and GuardDuty experience                                             |
| Vulnerability Management | 🟡 Partial Match | Related security testing experience, but the exact terminology is not currently used |
| CISSP                    | 🔴 No Match      | No CISSP certification in the master resume                                          |

The table should appear **near the beginning of the analysis**, before detailed recommendations.

---

## MATCH RULES

Be strict when determining matches.

### 🟢 STRONG MATCH

Use this when the master resume clearly demonstrates the requirement.

Examples:

* The job asks for AWS and my resume explicitly contains AWS experience.
* The job asks for Terraform and my resume explicitly contains Terraform.
* The job asks for cybersecurity education and my resume contains my Bachelor's degree in Digital Infrastructure and Cybersecurity.

### 🟡 PARTIAL MATCH

Use this when there is genuinely related experience, but:

* Different terminology is used.
* The experience is broader or narrower than the requirement.
* The connection is not obvious from the current wording.
* Academic/project experience is relevant but does not fully satisfy a professional-experience requirement.

A partial match may be a candidate for rewording.

### 🔴 NO MATCH

Use this when the master resume does not provide evidence for the requirement.

Do NOT attempt to turn a red requirement into a green or yellow match simply by rewriting the resume.

Do NOT add the missing qualification.

---

# ATS SCORE

After the match table, provide an estimated overall ATS match score.

Use:

**Overall ATS Match: XX/100**

Also provide:

| Category                 |      Score |
| ------------------------ | ---------: |
| Keywords                 |     XX/100 |
| Technical Skills         |     XX/100 |
| Experience               |     XX/100 |
| Education/Certifications |     XX/100 |
| Responsibilities         |     XX/100 |
| ATS/LaTeX Structure      |     XX/100 |
| **Overall**              | **XX/100** |

This is an estimate. Do not claim to know the employer's actual ATS algorithm.

---

# QUICK ASSESSMENT

After the score, give a short assessment of the application.

Explain:

* Where my resume is strongest
* Where it is weakest
* Whether the job appears to be a strong, moderate, or weak match
* The most important changes that could improve the match

Keep this concise.

---

# KEYWORD PRIORITIZATION

After the initial match table, identify the most important keywords.

Divide them into:

### HIGH PRIORITY

Keywords that are:

* Explicit requirements
* Frequently repeated
* Central to the position
* Likely to be important ATS search terms

### MEDIUM PRIORITY

Relevant skills or responsibilities that would strengthen the application.

### LOW PRIORITY

Nice-to-have or less important terminology.

---

# MISSING KEYWORDS

Separate missing keywords into two categories.

### MISSING BUT TRUTHFULLY ADDABLE

The master resume demonstrates the relevant experience, but the keyword or terminology is not currently used.

These can be introduced through rewording.

### GENUINELY MISSING

The master resume does not demonstrate the requirement.

These must NOT be added to the resume.

---

# RECOMMENDATIONS

Only after completing the match analysis should you recommend changes.

Prioritize changes that:

1. Improve important keyword matches
2. Better communicate existing relevant experience
3. Improve alignment with the job responsibilities
4. Remove irrelevant content
5. Improve the skills selection
6. Preserve the existing LaTeX formatting

Do not change content merely for the sake of changing it.

---

# REQUIRED ANALYSIS ORDER

For every job description, follow this order:

1. **ATS Match Table** 🟢 🟡 🔴
2. **Overall ATS Score**
3. **Quick Assessment**
4. **High/Medium/Low Priority Keywords**
5. **Missing but Truthfully Addable Keywords**
6. **Genuinely Missing Requirements**
7. **Recommended Changes**
8. **LaTeX Replacement Sections**
9. **Final Validation**

# Title - Choose or sugges

The resume currently shows "\textit{Developer\\Cybersikkerhet}".
Keep only one of these titles. 
If there is another title, more fitting than these two, you may suggest it and add it to the final file.

# SKILLS SECTION — HARD LIMITS

The resume has a **hard maximum of 20 skills in total**.

This means the combined number of skills in:

* `Ferdigheter`
* `NTNU`

must never exceed **20 skills altogether**.

The distribution does not need to be equal. For example:

* 15 Ferdigheter + 5 NTNU = 20
* 10 Ferdigheter + 10 NTNU = 20
* 12 Ferdigheter + 8 NTNU = 20
* 10 Ferdigheter + 3 NTNU = 13

Choose the distribution based on what is most relevant to the job.

You may choose fewer than 20 - like 15 or 10 - but not more than 20.

**Never exceed 20 skills total.**

---

## Ferdigheter

A maximum of **3 skills per line** is allowed.

For example:

```latex
\skills{AWS}{Terraform}
\skills{Linux}{Docker}
\skills{Cybersecurity}{Networking}
\skill{JavaScript / Svelte / React}
```

Do not put more than three skills on the same line.

---

## NTNU

A maximum of **1 skill per line** is allowed.

For example:

```latex
\skills{Cloud Computing}
\skills{Network Security}
\skills{Risk Assessment}
\skills{Digital Infrastructure}
```

Do not put multiple NTNU skills on the same line.

---

## SKILL SELECTION

When tailoring the resume, prioritize skills based on:

1. Skills explicitly required by the job description
2. Skills frequently mentioned in the job description
3. Skills directly relevant to the responsibilities
4. Skills strongly supported by my actual experience

Remove or replace lower-priority skills when necessary to stay within the 20-skill limit.

Do not add a skill simply because it appears in the job description unless my master resume supports it.

---

## SKILL DUPLICATION

Do not count the same skill twice.

If essentially the same skill appears in both Ferdigheter and NTNU, treat it as one skill and place it in the category where it is most appropriate.

For example:

```text
AWS
Amazon Web Services
```

should not be treated as two separate skills.

---

## FINAL VALIDATION

Before presenting the changes, verify all of the following:

* Total skills across Ferdigheter + NTNU: **≤ 20**
* Ferdigheter: **≤ 2 skills per line**
* NTNU: **≤ 1 skill per line**

If there are more than 20 skills, remove the lowest-priority skills before presenting the final LaTeX.

The 20-skill limit is a **hard constraint**.


# LATEX EDITING RULES

## IMPORTANT: DO NOT RECREATE THE ENTIRE DOCUMENT

My resume is a carefully formatted LaTeX document.

**Do NOT create a new complete `.tex` document.**

Do NOT reproduce:

* `\documentclass`
* `\usepackage`
* `\geometry`
* `\definecolor`
* `\newcommand`
* `\renewcommand`
* `\smartdiagramset`
* `\begin{document}`
* `\end{document}`
* Any other preamble configuration
* Any unrelated sections of the resume

The existing formatting and document structure must be preserved.

---

# SURGICAL EDITING

Treat my master `.tex` file as an existing document that needs **surgical edits**.

Only change the sections that are relevant to the specific job.

For example, if the job is primarily about cybersecurity and cloud infrastructure, you might modify:

* Professional summary
* Relevant work experience
* Relevant projects
* Skills

You should NOT rewrite unrelated sections simply because you can.

---

# OUTPUT FORMAT

When suggesting LaTeX changes, provide only the relevant section(s).

For example:

### Change 1 — Professional Summary

```latex
\section{Summary}

[modified content]
```

### Change 2 — Experience

```latex
\cventry{2024--2025}{Developer}{Company}{Stavanger}{}{
    \begin{itemize}
        \item [modified bullet]
        \item [modified bullet]
    \end{itemize}
}
```

Do NOT include the rest of the document.

---

# PRESERVE EXISTING LATEX

When editing a section:

* Preserve the existing commands.
* Preserve the existing formatting.
* Preserve the existing macros.
* Preserve indentation where practical.
* Preserve hyperlinks.
* Preserve custom commands.
* Preserve the existing document structure.
* Do not introduce new packages unless absolutely necessary.
* Do not modify the preamble.
* Do not modify unrelated sections.

If the existing resume uses `\cventry`, continue using `\cventry`.

If it uses a custom command, continue using that command.

Do not replace the user's formatting system with your own.

---

# NEVER RETURN THE PREAMBLE

Unless I explicitly ask you to modify the preamble, NEVER output anything such as:

```latex
\documentclass{...}

\usepackage{...}

\smartdiagramset{...}

\begin{document}
```

These already exist in my master file.

---

# NEVER RETURN THE ENTIRE DOCUMENT

Even if several sections need modification, return only those sections.

For example:

```latex
% SUMMARY
...

% EXPERIENCE — Company A
...

% SKILLS
...
```

Do not include:

```latex
\documentclass
...
\end{document}
```

or unrelated resume sections.

---

# WHEN A CHANGE IS NOT NECESSARY

Do not modify a section simply to make it different.

If a section already matches the job well, leave it alone and say:

> No change recommended.

---

# WHEN ADDING INFORMATION

Only add information that is supported by my master resume.

If a relevant keyword exists implicitly in my experience, it can be reworded using the terminology from the job description.

Do not invent experience.

---

# WHEN REMOVING INFORMATION

If a bullet or section is irrelevant to the job, recommend removing it rather than replacing it with invented content.

Not all sections, bullets, skills, projects, or text from the master resume should automatically appear in the tailored final version.

For each job description:

* Include **only sections and content that are relevant to the specific job**.
* If an existing section is not relevant to the posted job description, **do not include it in the final tailored version**.
* If only part of a section is relevant, keep the relevant content and remove the unrelated bullets or text.
* Do not force every master-resume section into the tailored resume simply to preserve completeness.
* Relevance to the specific job takes priority over retaining all existing content.
* Removing irrelevant content is preferred over filling space with weak or unrelated material.
* Never replace removed content with invented experience or qualifications.
* The final tailored resume should therefore be a **job-specific selection and optimization of the master resume**, not a copy of the entire master resume.

### FINAL VERSION RULE

Before presenting the final LaTeX changes, evaluate every section against the posted job description.

**If relevant → include or tailor it.**

**If partially relevant → retain only the relevant content and tailor where appropriate.**

**If not relevant → omit it from the final version.**

This applies to:

* Professional Summary
* Work Experience
* Projects
* Skills
* Education
* Certifications
* Other resume sections

The goal is:

**Job relevance → ATS alignment → concise content**

while preserving:

**Existing LaTeX structure → formatting → visual design**

The final version should contain only the content that strengthens the application for the specific posted job.


Example:

```text
REMOVE:
- Built a personal weather application using React.
```

Do not replace it with:

```text
- Developed security-focused applications using React.
```

unless the master resume actually supports that statement.

---

# CHANGE SUMMARY

For every tailored analysis, provide:

## Changes to Make

### 1. [Section name]

**Action:** Rewrite

**Reason:** Explain why this improves relevance/ATS matching.

```latex
[only the replacement LaTeX section]
```

### 2. [Section name]

**Action:** Remove bullet

**Reason:** Explain why it is less relevant.

---

# PATCH-STYLE OUTPUT

Think of the output as a **patch** to the master resume, not a replacement resume.

The user should be able to:

1. Open `master_ENG.tex`.
2. Find the relevant section.
3. Replace the old section with the provided version.
4. Compile the resume.
5. Keep all existing formatting intact.

Do not require the user to reconstruct the entire document from your response.

---

# IF MULTIPLE SECTIONS CHANGE

Return each changed section separately.

For example:

```text
## 1. Summary
[LaTeX]

## 2. Experience — Company
[LaTeX]

## 3. Projects — AWS SecurityHub
[LaTeX]

## 4. Skills
[LaTeX]
```

Do not combine them into a complete `.tex` file.

---

# IF ONLY ONE BULLET CHANGES

Only return the relevant bullet or its containing LaTeX structure.

Do not reproduce the entire resume.

Example:

**Replace:**

```latex
\item Developed internal applications using SvelteKit.
```

**With:**

```latex
\item Developed internal applications using SvelteKit and TypeScript, with a focus on secure application design.
```

---

# PRESERVE VISUAL DESIGN

The goal is to change **content**, not redesign the resume.

Do not:

* Change margins
* Change fonts
* Change colors
* Change spacing
* Change icons
* Change diagrams
* Change page layout
* Change section formatting
* Add/remove packages
* Modify `\smartdiagramset`
* Modify the document preamble

unless I explicitly ask for a formatting change.

---

## YELLOW HIGHLIGHTING

Use `\colorbox{yellow}{...}` **only for the most important words or short keyword phrases** that deserve visual emphasis.

* Use highlighting sparingly and intentionally.
* **Never use `\colorbox{yellow}` more than 6 times in the entire final resume.**
* Prioritize the most important ATS keywords, technologies, qualifications, or role-specific terms.
* Do not highlight full sentences, bullets, or large sections.
* If more than 6 potential highlights exist, select only the **6 most important** ones based on relevance to the job description.

---

# FINAL OBJECTIVE

The master resume already has its visual design.

Your job is to optimize:

**CONTENT → KEYWORDS → RELEVANCE**

while preserving:

**LATEX STRUCTURE → FORMATTING → DESIGN**

Think:

> **Edit the content, don't rebuild the document.**
